# Supabase 프로젝트 설정
project_id = "ruqeinfcqhgexrckonsy"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323
api_url = "http://localhost"

[auth]
enabled = true
site_url = "http://localhost:3000"

# Edge Functions 설정
[functions.gemini-proxy]
verify_jwt = false  # JWT 인증 비활성화 (클라이언트 직접 호출 허용)

[functions.daily-briefing]
verify_jwt = true  # 배치 작업은 인증 필요
